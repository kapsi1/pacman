{
  "name": "pacman",
  "type": "module",
  "scripts": {
    "predev": "rm -rf dist",
    "dev": "parcel ./src/index.html",
    "prebuild": "rm -rf dist",
    "build": "parcel build ./src/index.html --public-url /pacman/"
  },
  "dependencies": {
    "@parcel/config-default": "^2.14.4",
    "parcel": "^2.14.4"
  }
}
